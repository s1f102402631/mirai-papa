"use client";
import AppShell from "../../components/AppShell";
import { actions } from "../../data/content";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { useState } from "react";
export default function Actions(){const [done,setDone]=useState<number[]>([]);return <AppShell><div className="mx-auto max-w-3xl py-5 sm:py-12"><p className="text-xs font-black tracking-[.2em] text-[#ef7548]">START SMALL</p><h1 className="mt-3 text-4xl font-black text-[#163b68]">今からできること</h1><p className="mt-4 text-sm leading-7 text-slate-500">大学生の今だからこそできる、小さな一歩を選んでみましょう。</p><div className="mt-8 grid gap-4">{actions.map((a,i)=><button key={a.title} onClick={()=>setDone(d=>d.includes(i)?d.filter(x=>x!==i):[...d,i])} className={`flex items-start gap-4 rounded-3xl border-2 p-5 text-left transition hover:-translate-y-0.5 ${done.includes(i)?"border-[#ef7548] bg-orange-50":"border-white bg-white shadow-md"}`}><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-slate-50 text-2xl">{a.icon}</span><span className="flex-1"><span className="block font-black text-[#163b68]">{a.title}</span><span className="mt-1 block text-sm leading-6 text-slate-500">{a.text}</span></span><CheckCircle2 className={done.includes(i)?"text-[#ef7548]":"text-slate-200"}/></button>)}</div><div className="mt-8 rounded-3xl bg-[#163b68] p-6 text-white"><p className="font-black">{done.length}つ選択中</p><p className="mt-2 text-sm leading-6 text-blue-100">できそうなことを一つ選ぶだけでも、未来を考えるきっかけになります。</p></div><a href="/messages" className="mt-6 flex items-center justify-center gap-2 rounded-2xl bg-[#1769c2] px-6 py-4 font-black text-white">未来の家族からのメッセージ <ArrowRight size={18}/></a></div></AppShell>}
